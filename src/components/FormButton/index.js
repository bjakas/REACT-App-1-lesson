import "./FormButton.css";

export default function FormButton(props) {
  return <button {...props} className="FormButton" /> // sve što ćemo dodati buttonu dodat ćemo direktno; classa će na svakoj komponenti omogućiti isti stil
}

